let a = true
let b = true
let c = false

if a then
    if b and c then
        echo("A and B and C")
    else
        echo("A, but no B and C")
    end

    echo(a)
    echo(b)
    echo(c)
else
    echo("no A")
end