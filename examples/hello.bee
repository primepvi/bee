const ax: int = 10
const ay = 10

lit bx = 20
const by: lit int = 30

fn sum(x: lit int, y: lit int): lit int do
   return x + y
end

lit result = sum(ax * bx, ay * by)
echo result

const hello: atom = :hello
echo hello
